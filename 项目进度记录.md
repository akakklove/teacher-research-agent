# 教师个人科研查询器 — 项目文档

> 最后更新：2026-07-10  
> 当前版本：v0.4.0 — 多轮对话 + 场景模板 + 模板复用 + 指标发现  
> 仓库：`teacher-research-agent/`

---

## 第一部分：项目介绍

### 1.1 背景与问题

高校教师的科研数据分散在多个系统（科研管理系统、财务系统、人事系统）。教师想了解自己的科研全貌——主持了多少项目、经费到账了多少、发表了几篇论文、获得了什么奖项——往往需要：

1. 登录不同系统逐个查询
2. 手工整理成 Excel 或 PPT
3. 遇到系统间数据不一致时反复核对

一次年度总结或职称评审材料准备，耗时数天。

### 1.2 产品定位

**一句话**：教师登录后，用自然语言问一问，系统自动生成一份个人科研全景大屏报告。

**目标用户**：
- 一线教师（查个人科研数据、做年度总结、准备职称评审材料）
- 院系领导（查看本院教师科研产出对比）
- 科研管理部门（全校科研数据总览）

**核心价值**：把"登录 5 个系统 + 手工整理 3 天"压到"说一句话 + 等 3 秒"。

### 1.3 产品形态

```
教师端：
├── 登录（工号 + 密码）
├── 自然语言对话（"我近三年的科研情况怎么样"）
├── 场景模板（年度总结 / 职称评审 / 基金申报支撑）
├── 指标超市（手动勾选想要的指标）
├── 大屏预览（ECharts 图表 + AI 洞察文字）
├── 导出功能（PPT / PDF / Word 表格）
└── 模板保存与复用

超管端（V1.0）：
├── 数据源配置（MySQL / Oracle / CSV）
├── 字段映射配置（源字段 → 标准字段的 ETL 映射）
├── 定时同步调度 + 日志监控
└── 用户管理
```

### 1.4 技术架构（五层）

```
┌─────────────────────────────────────────────┐
│  展示层    HTML大屏 + Chat UI + 指标选择器     │  ← ✅ V0.3/V0.4
├─────────────────────────────────────────────┤
│  Agent层   意图路由 + 指标引擎 + 洞察 + 编排   │  ← ✅ V0.2/V0.4
│            场景模板 + 模板存储 + 指标发现      │
├─────────────────────────────────────────────┤
│  服务层    FastAPI + 数据接口 + 安全校验      │  ← ✅ V0.2
├─────────────────────────────────────────────┤
│  数据层    MySQL + 指标引擎 + Schema探索     │  ← ✅ V0.1
├─────────────────────────────────────────────┤
│  治理层    Schema Registry + Mock Generator  │  ← ✅ V0.1
└─────────────────────────────────────────────┘
```

**关键设计决策**：19 张应用层表 = 固定数据契约。源系统（科研管理系统、财务系统）的变化通过 ETL 映射层隔离，Agent 层和展示层完全不感知源系统长什么样。

### 1.5 数据模型

以 `t_ky_xmry`（项目人员关系表）为核心枢纽表：

```
t_jzg_jbxx (教师) ──→ t_ky_xmry (项目人员) ←── t_ky_xmjbxx (项目)
                           │
          ┌────────────────┼────────────────┐
          ↓                ↓                ↓
    经费到账/支出/外拨    成果(论文/专利/   活动(会议/机构)
                          著作/软著/获奖)
```

所有以教师为维度的查询，都从 `t_ky_xmry` 出发，通过 `kybh`（项目编号）关联到项目的经费、成果、活动数据。

---

## 第二部分：当前进度 — v0.4.0

### 总体进度

```
数据治理层  ████████████████████  100%  ✅ (v0.1)
数据存储层  ████████████████████  100%  ✅ (v0.1)
指标层      ████████████████████  100%  ✅ (v0.2)
服务层      ████████████████████  100%  ✅ (v0.2)
Agent层     ████████████████████  100%  ✅ (v0.3/v0.4)
展示层      ████████████████████  100%  ✅ (v0.3/v0.4)
─────────────────────────────────────
总体        ████████████████████  100%  ✅
```

### 2.1 已完成 — 数据治理层

**Schema 注册中心**：
- 从高校科研管理数据标准 Excel 解析出全部 15 个数据集、279 张表、3,636 个字段
- 每个数据集独立存储为 JSON，包含完整字段注释、类型、主键、非空约束、业务描述
- 自动检测出 12 条跨表关联关系

**Mock 数据生成器**：
- 50 名教师、200 个项目、300 篇论文、80 项专利、40 本著作、50 项软著、60 项获奖、30 场学术会议、15 个科研机构
- 总计 4,252 条数据，覆盖 19 张表的完整关联
- 固定随机种子（SEED=42），每次运行生成完全相同的数据，方便调试
- 内置业务特征规则：教授更容易拿国家级项目、计算机学院论文产出高于文科学院、经费按学院系数加权

### 2.2 已完成 — 数据存储层

**Docker MySQL 8.0 环境**：

| 配置项 | 值 |
|--------|-----|
| 地址 | `127.0.0.1:3307` |
| 数据库 | `teacher_research` |
| 用户 | `tr_user` / `tr_pass_2025` |
| 管理员 | `root` / `root123456` |
| 编码 | `utf8mb4_unicode_ci`（全链路） |

**19 张应用层表**：

| 类别 | 表数量 | 核心表 |
|------|--------|--------|
| 教师信息 | 1 | `t_jzg_jbxx` |
| 科研项目 | 2 | `t_ky_xmjbxx` + `t_ky_xmry`（枢纽） |
| 科研经费 | 3 | `t_ky_jfdz` / `t_ky_jfzc` / `t_ky_jfwb` |
| 科研成果 | 10 | 论文/专利/著作/软著/获奖 + 对应人员关联表 |
| 学术活动 | 3 | `t_ky_xshy` / `t_ky_kyjg` / `t_ky_kyjgry` |

**DataSource Interface 设计**：已定义抽象接口，目前 SQLite 和 MySQL adapter 待实现。

**快速启动**：
```bash
# 启动 MySQL
cd teacher-research-agent/docker
docker compose up -d

# 灌入 Mock 数据
cd teacher-research-agent/data_governance/mock_generator
python seeder.py
```

### 2.3 已完成 — v0.2.0 指标层 + 服务层

**30 个核心指标** (`agent/metrics.yaml`)：

| 模块 | 指标数 | 示例 |
|------|:--:|------|
| 项目概览 | 5 | project_count_leader, project_by_level, project_yearly_trend |
| 经费分析 | 6 | fund_total_arrived, fund_execution_rate, fund_monthly_trend |
| 论文成果 | 5 | paper_count_total, paper_by_level, paper_author_role |
| 专利成果 | 3 | patent_count, patent_by_type, patent_yearly_trend |
| 著作/软著 | 4 | book_count, book_by_type, software_count, software_yearly_trend |
| 获奖荣誉 | 3 | award_count, award_by_level, award_timeline |
| 学术活动 | 2 | conference_hosted, conference_total_papers |
| 机构参与 | 2 | institution_membership |

**FastAPI 服务** (`data_service/api.py`)：
- `GET /api/health` — 数据库联通检查
- `GET /api/metrics` — 指标目录（支持分类筛选）
- `GET /api/teacher/{id}` — 教师基本信息
- `GET /api/teacher/{id}/overview` — 完整指标查询（JSON）
- `GET /api/teacher/{id}/report` — ECharts 大屏（HTML）
- `GET /api/teacher/{id}/chat` — 对话式查询（JSON）

**技术选型**：通义千问 (qwen-max) × LangChain 轻量封装（ChatModel + PromptTemplate + OutputParser），不依赖 Dify。

### 2.4 已完成 — v0.3.0 Agent 层 + 展示层

**意图路由** (`agent/intent_router.py`)：11 种意图（含追问意图），LLM + 规则双模式

**洞察引擎** (`agent/insight_engine.py`)：数据 → 自然语言洞察，LLM + 阈值规则双模式

**大屏编排** (`agent/dashboard_composer.py`)：
- 支持 6 种图表：kpi_card, line_chart, bar_chart, pie_chart, horizontal_bar, gauge, table
- CustomEncoder 处理 MySQL Decimal/date 序列化
- 1920×1080 深色主题，3 列布局，26 指标（11 KPI + 15 图表）

### 2.5 已完成 — v0.4.0 多轮对话 + 场景模板

**多轮对话记忆** (`agent/conversation_memory.py`)：
- 会话持久化（session_id 机制，TTL 3600s）
- 追问检测：代词消解（那/还有/再）+ 主题词推断（论文→paper_analysis）
- 上下文继承：时间范围、教师ID、对话历史摘要
- API：`POST /api/chat/send`（支持 session_id 继续会话）

**场景模板引擎** (`agent/scenario_templates.py`)：
- 📝 年度科研总结（12 指标，36 月，含院系对比）
- 🏅 职称评审材料（11 指标，60 月，成果量化）
- 💰 基金申报支撑（8 指标，36 月，前期基础论证）
- LLM 叙事文案生成 × 规则兜底
- API：`POST /api/chat/scenario`

**模板保存/复用** (`agent/template_store.py`)：
- SQLite 持久化（data/templates.db）
- 增删改查：save / list / get / rename / delete
- API：`GET/POST/DELETE/PATCH /api/templates/...`
- Chat UI：「💾 保存为模板」+「📋 我的模板」面板

**指标自动发现** (`agent/metric_discovery.py`)：
- Schema 探索器：加载 GXKY 19 表结构，生成 LLM 友好摘要
- 双模式：LLM SQL 生成 + 多关键词分词搜索兜底
- 表名映射：SCHEMA_TO_MYSQL 精确对应 19 张应用层表
- API：`POST /api/chat/discover`

**Chat 交互界面** (`dashboard/templates/chat.html`)：
- 教师选择登录 → 气泡对话 → KPI 卡片嵌入 → AI 洞察展示
- 快捷入口：科研全景、论文分析、经费详情
- 场景按钮：年度总结（橙）、职称评审（红）、基金申报（绿）
- 模板管理 + 指标探索

### 2.6 项目结构（当前）

### 2.6 项目结构（当前）

```
teacher-research-agent/
├── docker/                         # Docker MySQL 环境
│   ├── docker-compose.yml
│   └── init.sql                    # 19 张表建表脚本
├── data_governance/                # ✅ 数据治理层
│   ├── schema_registry/            #   15 数据集 JSON Schema
│   └── mock_generator/             #   Mock 生成 + 灌入器
├── data_service/                   # ✅ 服务层
│   └── api.py                      #   FastAPI 全部路由
├── agent/                          # ✅ Agent 编排层
│   ├── metrics.yaml                #   30 个指标定义
│   ├── metric_engine.py            #   指标查询引擎
│   ├── intent_router.py            #   意图路由（11 种意图）
│   ├── insight_engine.py           #   AI 洞察生成
│   ├── dashboard_composer.py       #   大屏编排 + ECharts
│   ├── conversation_memory.py      #   v0.4 会话记忆
│   ├── scenario_templates.py       #   v0.4 场景模板
│   ├── template_store.py           #   v0.4 模板存储
│   ├── metric_discovery.py         #   v0.4 指标发现
│   ├── llm.py                      #   通义千问初始化
│   └── prompts/                    #   LLM Prompt 模板（4 个）
├── dashboard/                      # ✅ 展示层
│   └── templates/
│       ├── dashboard.html          #   1920×1080 ECharts 大屏
│       └── chat.html               #   v0.4 聊天互动界面
├── main.py                         # Uvicorn 启动入口
├── start.bat                       # Windows 启动脚本
├── .env                            # API Key（gitignored）
├── .gitignore
└── 项目进度记录.md
```

---

## 第三部分：版本迭代计划

### v0.2.0 — 指标层 + 服务层 ✅ 已完成

**目标**：输入教师工号 → 返回完整的指标查询结果

```
交付物：
├── data_store/interface.py          # DataSourceInterface + SQLite adapter
├── data_service/
│   ├── api.py                       # FastAPI 路由（/api/query 等）
│   ├── metadata_cache.py            # Schema 内存缓存
│   └── security.py                  # SQL 白名单 + 只读限制
└── agent/
    ├── metrics.yaml                 # 30 个指标定义（名称 + SQL + 数据来源 + 推荐图表）
    └── metric_engine.py             # 指标查询引擎
```

**30 个核心指标**：

| 模块 | 指标示例 |
|------|----------|
| 项目概览（5个） | 主持项目数、项目级别分布、项目来源分布、项目状态分布、年度立项趋势 |
| 经费分析（6个） | 到账经费总额、月度到账趋势、经费执行率、支出结构、年均经费对比 |
| 论文成果（5个） | 发表论文数、论文级别分布（SCI/EI/核心）、年度论文趋势、第一作者排名 |
| 专利成果（3个） | 专利总数、专利类型分布、年度申请趋势 |
| 著作/软著（4个） | 著作数、软著数、出版社分布、年度趋势 |
| 获奖荣誉（3个） | 获奖总数、获奖级别分布、获奖类别分布 |
| 学术活动（2个） | 学术会议参与次数、特邀报告次数 |
| 对比分析（2个） | 院系均值对比、同职称教师排名 |

**验收标准**：
```python
engine = MetricEngine(datasource, schema_registry)
result = engine.execute(teacher_id="GH20200001", time_range=("2022-01-01","2025-12-31"))
# result.metrics = { "project_count": 8, "fund_total": 3125000, "paper_count": 12, ... }
```

### v0.3.0 — Agent 层 + 大屏展示 ✅ 已完成

**目标**：自然语言提问 → 大屏 HTML 报告

```
交付物：
├── agent/
│   ├── intent_router.py             # 意图识别 + LLM Prompt
│   ├── insight_engine.py            # 数据 → 自然语言洞察（趋势/异常/建议）
│   ├── dashboard_composer.py        # 指标列表 → ECharts 配置 → HTML
│   └── prompts/                     # LLM 提示模板
├── dashboard/
│   ├── templates/dashboard.html     # ECharts 大屏 HTML 模板
│   ├── chart_registry.py            # 图表类型注册
│   └── layout_engine.py             # 智能布局引擎
└── main.py                          # FastAPI 启动入口
```

**交互流程**：
```
教师登录 → 输入"帮我查科研情况"
    → 意图识别：personal_overview, time=近三年
    → 指标推荐：项目概览 + 经费分析 + 论文成果（默认组合）
    → 批量查询：7 条 SQL 并行执行
    → 洞察生成：LLM 生成 2-3 条业务洞察
    → 大屏渲染：ECharts KPI卡片 + 折线图 + 饼图 + 洞察卡片
    → 输出：完整 HTML 报告页面
```

**大屏布局**：
```
┌──────────────────────────────────────────┐
│  🔬 张建国 个人科研全景 (2023-2025)        │
│                                            │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│  │ 8项目 │ │520万  │ │ 12篇 │ │ 3项奖 │    │  ← KPI 卡片
│  └──────┘ └──────┘ └──────┘ └──────┘    │
│                                            │
│  ┌───────────────┐ ┌───────────────┐     │
│  │ 经费月度趋势   │ │ 论文级别分布   │     │  ← 图表区
│  └───────────────┘ └───────────────┘     │
│                                            │
│  ┌──────────────────────────────────┐    │
│  │ 💡 智能洞察                       │    │  ← 洞察区
│  │ · 经费年均增长34%                 │    │
│  │ · 论文产出高于院系均值42%          │    │
│  │ · ⚠️ 2个项目即将到期              │    │
│  └──────────────────────────────────┘    │
└──────────────────────────────────────────┘
```

### v0.4.0 — 场景模板 + 对话式交互 ✅ 已完成

**目标**：不只是查数据，而是为具体场景服务

```
新增能力：
├── 场景模板引擎
│   ├── "年度科研总结" → 预设 12 个指标 + 趋势叙事
│   ├── "职称评审材料" → 预设 10 个指标 + 成果量化
│   └── "基金申报支撑" → 预设 8 个指标 + 前期基础展示
├── 追问对话
│   ├── "只看2025年的" → 时间过滤
│   ├── "对比一下李老师" → 对比模式
│   └── "导出PPT" → 一键导出
├── 模板保存/复用
│   └── 搭好的大屏存为个人模板，下次一键更新数据
└── 指标自动发现
    └── 用户想要的指标不在注册表中时，从 Schema 自动探索
```

### v1.0.0 — 超管端 + ETL + 产品化（预计 3-4 周）

**目标**：从 Demo 变为可部署的产品

```
新增能力：
├── 超管后台
│   ├── 数据源管理（MySQL/Oracle/CSV 连接配置）
│   ├── 字段映射配置（源字段 → 标准字段，含编码转换规则）
│   ├── ETL 任务调度（Cron / 手动触发 / 增量/全量）
│   └── 同步日志 + 失败告警
├── 用户管理
│   ├── 教师账号管理
│   ├── JWT 登录 + 角色权限（教师/超管）
│   └── 查询日志 + 用量统计
├── 导出功能
│   ├── PPT 导出（python-pptx）
│   ├── PDF 报告
│   └── Word 表格（职称评审材料常用）
└── 数据新鲜度
    └── 每个指标标注数据来源、更新频率、统计口径
```

### v1.x+ — 未来规划

| 版本 | 功能 | 价值 |
|------|------|------|
| v1.1 | 多数据集扩展（教学/学生/财务） | 产品线扩展 |
| v1.2 | 跨域关联大屏（科研+财务+人事） | 数据标准核心价值 |
| v1.3 | 指标需求工单 + 管理员扩展后台 | 系统可运营 |
| v1.4 | 院系/全校维度大屏 | 服务管理层用户 |
| v2.0 | 多租户（支持多所高校部署） | SaaS 化 |

---

## 附录

### A. 常用命令

```bash
# 启动 MySQL
cd docker && docker compose up -d

# 灌入 Mock 数据（首次需要）
cd data_governance/mock_generator && python seeder.py

# 启动服务
python main.py
# 或
start.bat

# 访问
# 大屏：http://127.0.0.1:8000/api/teacher/GH20200001/report
# 聊天：http://127.0.0.1:8000/api/chat

# 连接数据库
mysql -h127.0.0.1 -P3307 -utr_user -ptr_pass_2025 teacher_research

# 重启并重新初始化（⚠️ 会清空数据）
cd docker && docker compose down -v && docker compose up -d
```

### B. 设计备忘

- 表名全部小写，MySQL 8.0 兼容
- 编码全链路 utf8mb4，已解决 Navicat 乱码（需 `skip-character-set-client-handshake`）
- 核心枢纽表 `t_ky_xmry`，通过 `ry_gh` 关联教师，通过 `kybh` 关联所有项目/成果
- Mock 数据固定种子（SEED=42）保证可复现
- 19 张表 = 应用层固定 Schema，源系统变化通过 ETL 映射隔离
- 产品话语中统一使用"高校科研管理数据标准"
